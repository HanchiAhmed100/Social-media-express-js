<% include header %>
	<div class="row">
		<div class="col-md-12">
				<% if( user != null) {%>
		    <form action="/Acceuil/" method="post">
		    	<label><%= user.name %></label><br><hr>
		        <label>Title : </label>
		        <input type="text" name="title" id="title" class="uk-input uk-margin"><br>
		        <label>Body : </label>
		        <textarea class="uk-textarea uk-margin" name="body" id="body"></textarea><br>
		        <button type="submit" class="uk-button uk-button-default">Envoyer</button>
		    </form>
		    	<% } %>
		</div>
	</div>
<sc


	<div class="uk-margin">
		<div class="row">
			<% for (msg of message) { %>

			<div class="col-sm-1">
				<img src="/assets/ProfilesPictures/<%= msg.picture %>">
			</div>
			<div class="col-sm-11">
	            <h4>Title : <a href="/Acceuil/<%= msg.id %>"><%= msg.title %></a></h3>
					<h5>By : <a href="/ViewProfile/<%= msg.UserId %>"><%= msg.name %> - <%= msg.email %></a></h5>
	            <p><%= msg.body %></p>
	            <input type="hidden" value="<%= msg.id  %>" id=myid>
	            <b><small class="pull-right"><%= msg.dateof.fromNow() %></small></b>
	            <br>
	            <hr>
        	</div>

			<% } %>
		</div>
	</div>

        	<script type="text/javascript">
        					$(document).ready(function() {
        		var x = [];
        		var x = document.querySelectorAll("#myid").value;
        		for (var i = 0; i < x.length; i++) {
        			console.log(x[i]);
        		}

			})
        	</script>
<% include footer %>